# DISABLED: tenant statistics hotfix

Do not apply this patch or any kit previously generated by its factory. Every
generation and execution entry point exits with `PATCH_114_FACTORY_DISABLED`.

The factory is incomplete. It creates only the table, API Lambda update, and API
Gateway route. It omits the tenant-stats writer Lambda, writer IAM and
environment, EventBridge schedule, and authenticated HTTP end-to-end test.

The route also hard-codes `authorization_type=NONE` with
`api_key_required=true`. That can bypass the platform `CUSTOM` authorizer in
platform-key mode.

Replace it with a complete patch that includes all missing resources, preserves
the live authorizer, and passes an authenticated HTTP end-to-end test before
re-enabling generation or execution.
